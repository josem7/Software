<%= form_with(model: campu, local: true) do |form| %>
  <% if campu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(campu.errors.count, "error") %> prohibited this campu from being saved:</h2>

      <ul>
      <% campu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :campu_nombre %>
  </div>

  <div class="field">
    <%= form.label :ubicacion %>
    <%= form.text_area :ubicacion, id: :campu_ubicacion %>
  </div>

  <div class="field">
    <%= form.label :mapa %>
    <%= form.text_area :mapa, id: :campu_mapa %>
  </div>

  <div class="actions">
    <%= form.submit "Crear Campus"%>
  </div>
<% end %>
